import TopCastFeaturesCompact from "../TopCastFeaturesCompact";

export default function CastSectionNew({ cast }) {
  // The new component handles all the logic internally
  return <TopCastFeaturesCompact cast={cast} />;
}
